server.port=${BACKEND_PORT:8080}

spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/${DB_NAME:global_threads_backend}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:Hemu@3433}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.sql.init.mode=never
